name: My First Workflow
on: push
jobs:
  say-hello:  #job name
    runs-on: ubuntu-latest  #os on which job will run

    steps:
      - name: Print message
        run: echo "Hello, Github Actions!"
      - name: Print name
        run: echo "Sanam"